const name = "name";
const middle = 'middle';
const last = `last`;

const sentence = "shes so \"cool\"";

/*
concatination:
joing strings together

example below
*/

const hello = 'hello my name is '; + name + "nice to meet you";

/*
Backtick syntax to easily concatenate strings and variables
${nameOfVariable} + "string";
*/

const hello2 = `hello my name is ${name}. Nice to Meet ya!`;